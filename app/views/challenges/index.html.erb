<div class="container">
  <div class="big">
    <% @challenges.each do |challenge| %>
      <div class="card">
            <div class="imgBx">
          <%= link_to challenge_path(challenge), style: "text-decoration: none" do %>
            <%=image_tag("6.jpeg")%>
            </div>
            <div class="contentBx">
                <div class="info-challenge d-flex ">
                  <p class="status-index">Active</p>
                  <p class="reward">💰 <%= challenge.reward.truncate(10) %></p>
                </div>
              <h2><%= challenge.name %></h2>
              <div class="size">
                <h3><%= challenge.description.truncate(30) %></h3>
              </div>
            </div>
          <% end %>
      </div>
    <% end %>
  </div>
</div>
